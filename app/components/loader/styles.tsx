/**
 * loader Styles
 * @format
 */

import { ScaledSheet } from '@app/styles';

export const getStyles = theme =>
  ScaledSheet.create({
    container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    },
  });
